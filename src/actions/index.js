import * as login from './login';
import * as session from './session';
import * as text from './text';

export {login as loginTypes} 
export const loginActions = login.default 

export {session as sessionTypes}
export const sessionActions = session.default 

export {text as textTypes}
export const textActions = text.default